services:
  authelia:
    image: authelia/authelia:latest
    container_name: authelia
    volumes:
      - /containers/authelia/config:/config
    networks:
      - proxy
    environment:
      - TZ=America/Los_Angeles
    restart: unless-stopped
    ports:
      - "9091:9091"

networks:
  proxy:
    external: true